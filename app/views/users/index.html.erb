<main>
  <div class="main-wrapper pt-80">
    <div class="container">
      <div class="row">
        <div class="col-lg-3 order-2 order-lg-1">
          <aside class="widget-area">
            <!-- widget single item start -->
            <div class="card card-profile widget-item p-0">
              <div class="profile-banner">
                <figure class="profile-banner-small">
                  <a href="#">
                    <img src="<%= asset_path 'banner-small' %>" alt="">
                  </a>
                  <a href="#" class="profile-thumb-2">
                    <img src="<%= asset_path 'profile-midle-1.jpg' %>" alt="">
                  </a>
                </figure>
                <div class="profile-desc text-center">
                  <h6 class="author"><a href="#"><%= current_user.name %></a></h6>
                  <p>Any one can join with but Social network us if you want Any one can join with us if you want</p>
                </div>
              </div>
            </div>
            <!-- widget single item start -->

            <!-- widget single item start -->
            <div class="card widget-item">
              <h4 class="widget-title">People you may know</h4>
              <div class="widget-body">
                <ul class="like-page-list-wrapper">
                  <% @recommendations.each do |user| %>
                  <li class="unorder-list">
                    <!-- profile picture end -->
                    <div class="profile-thumb">
                      <a href="#">
                        <figure class="profile-thumb-small">
                          <img src="<%= asset_path 'profile-small-3.jpg' %>" alt="profile picture">
                        </figure>
                      </a>
                    </div>
                    <!-- profile picture end -->

                    <div class="unorder-list-info">
                      <h3 class="list-title"><a href="<%= user_path(user.id) %>"><%= user.name %></a></h3>
                      <p class="list-subtitle"><a> <i class="fa fa-address-card" aria-hidden="true"></i> <%= user.job_title %></a></p>
                    </div>
                    <button class="like-button active">
                      <img class="heart" src="<%= asset_path 'heart.png' %>" alt="">
                      <img class="heart-color" src="<%= asset_path 'heart-color.png' %>" alt="">
                    </button>
                  </li>
                  <% end %>
                </ul>
              </div>
            </div>
            <!-- widget single item end -->

          </aside>
        </div>

        <div class="col-lg-6 order-1 order-lg-2">
          <%= render 'users/inquiry_section' %>

          <% @users.each do |user| %>
          <div class="card">
            <div class="post-title d-flex align-items-center">
              <!-- profile picture end -->
              <div class="profile-thumb">
                <a href="#">
                  <figure class="profile-thumb-large">
                    <img src="<%= asset_path 'profile-midle-1.jpg'%>" alt="profile picture">
                  </figure>
                </a>
              </div>
              <!-- profile picture end -->
              <div class="posted-author">
                <h6 class="author"><a href="<%= user_path(user.id) %>"><%= user.name %></a></h6>
                <span class="post-time"><i class="fa fa-map-marker" aria-hidden="true"></i> Location</span>
                <span class="post-time"><i class="fa fa-address-card" aria-hidden="true"></i> <%= user.job_title %></span>
              </div>
            </div>
          </div>
          <% end %>

          <div class="row">
            <div class="col-md-4"></div>
            <div class="col-md-4"><%== pagy_bootstrap_nav(@pagy) %></div>
          </div>

        </div>

        <%# if @requested_users.any? %>
          <div class="col-lg-3 order-3">
              <aside class="widget-area">
                <!-- widget single item start -->
                <div class="card widget-item">
                  <h4 class="widget-title">Network Requests (<%= @requested_users.count %>)</h4>
                  <div class="widget-body">
                    <ul class="like-page-list-wrapper">
                      <% @requested_users.each do |request| %>
                        <li class="unorder-list">
                          <!-- profile picture end -->
                          <div class="profile-thumb">
                            <a href="#">
                              <figure class="profile-thumb-small">
                                <img src="<%= asset_path 'profile-small-3.jpg' %>" alt="profile picture">
                              </figure>
                            </a>
                          </div>
                          <!-- profile picture end -->

                          <div class="unorder-list-info">
                            <h3 class="list-title"><a href="#"><strong><a href="<%= user_path(request.requester.id) %>"><%= request.requester.name %></a> </strong> requested to join your network</a></h3>
                            <p class="list-subtitle"><%= time_ago_in_words( request.created_at) %> ago</p>
                            <div class="row">
                              <%= link_to 'Accept', add_friend_path(id: request.requester.id), method: :post, class: "btn btn-primary col-md-5", style: "background-color: powderblue"  %>
                              <div class="col-md-1"></div>
                              <%= link_to 'Reject', reject_friend_path(id: request.requester.id), method: :post, class: "btn btn-primary col-md-5",  style: "background-color: orangered"  %>
                            </div>
                          </div>
                        </li>
                      <% end %>
                    </ul>
                  </div>
                </div>
                <!-- widget single item end -->

              </aside>
            </div>
        <%# end %>
      </div>
    </div>
  </div>

</main>